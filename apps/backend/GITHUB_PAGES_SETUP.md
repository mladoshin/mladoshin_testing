# Настройка GitHub Pages для Allure отчетов

## Автоматическая публикация отчетов

Workflow настроен на автоматическую публикацию Allure отчетов на GitHub Pages после каждого успешного запуска тестов в ветке `main`.

## Шаги настройки

### 1. Включите GitHub Pages в репозитории

1. Перейдите в Settings вашего репозитория
2. В левом меню выберите "Pages"
3. В секции "Build and deployment":
   - **Source**: выберите "Deploy from a branch"
   - **Branch**: выберите `gh-pages` и `/ (root)`
   - Нажмите "Save"

### 2. Проверьте permissions для GitHub Actions

1. Settings → Actions → General
2. Прокрутите до "Workflow permissions"
3. Выберите "Read and write permissions"
4. Отметьте "Allow GitHub Actions to create and approve pull requests"
5. Нажмите "Save"

### 3. Первый запуск

После первого push в ветку `main`:

1. GitHub Actions автоматически запустит тесты
2. После прохождения тестов будет сгенерирован Allure отчет
3. Отчет будет опубликован на GitHub Pages
4. Ветка `gh-pages` будет создана автоматически

### 4. Доступ к отчетам

После настройки отчеты будут доступны по адресу:

```
https://<your-username>.github.io/<repo-name>/
```

Например:
```
https://mladoshin.github.io/Testing/
```

## Структура отчетов

- **Latest Report**: `/` - последний отчет из main ветки
- **История**: Сохраняются последние 20 запусков
- **Тренды**: Автоматически отслеживаются изменения в результатах тестов

## Просмотр отчетов локально

Если вы хотите посмотреть отчеты локально после запуска тестов:

```bash
# Сгенерировать отчет
docker compose -f docker-compose.local.test.yml exec test-runner \
  allure generate allure-results -o allure-report --clean

# Открыть в браузере (требует установленного Allure)
allure open apps/backend/allure-report
```

Или без установки Allure:

```bash
# Просто откройте файл в браузере
open apps/backend/allure-report/index.html
```

## Артефакты в GitHub Actions

Для всех веток (включая pull requests) отчеты доступны как артефакты:

1. Перейдите в Actions → выберите workflow run
2. Прокрутите вниз до секции "Artifacts"
3. Скачайте `allure-report.zip`
4. Распакуйте и откройте `index.html` в браузере

## Troubleshooting

### Отчет не публикуется

**Проблема**: После push в main отчет не появляется на GitHub Pages

**Решение**:
1. Проверьте что workflow завершился успешно
2. Убедитесь что в Settings → Pages выбрана ветка `gh-pages`
3. Проверьте что permissions настроены корректно
4. Посмотрите логи в Actions → выберите workflow → шаг "Deploy to GitHub Pages"

### 404 Not Found при открытии отчета

**Проблема**: GitHub Pages URL возвращает 404

**Решение**:
1. Дождитесь 1-2 минуты после первой публикации
2. Проверьте что ветка `gh-pages` создалась
3. Убедитесь что в Settings → Pages правильно настроен source

### Отчет пустой или не содержит тестов

**Проблема**: Отчет открывается, но не содержит результатов

**Решение**:
1. Проверьте что тесты действительно запустились и прошли
2. Убедитесь что директория `allure-results` содержит JSON файлы
3. Посмотрите логи шага "Upload test results" в Actions

## Что включено в отчет

Allure отчет содержит:

- ✅ **Результаты тестов** - успешные, упавшие, пропущенные
- 📊 **Графики и диаграммы** - распределение по статусам
- 📈 **Тренды** - изменения результатов со временем
- 🏷️ **Категории** - группировка тестов по типам (unit, integration, e2e)
- ⏱️ **Временные метрики** - длительность выполнения
- 📝 **Описания** - подробная информация о каждом тесте
- 🔍 **История** - последние 20 запусков

## Пример отчета

После настройки вы увидите отчет примерно такого формата:

```
┌─────────────────────────────────────────┐
│ ALLURE REPORT                           │
├─────────────────────────────────────────┤
│ Total Tests: 427                        │
│ ✅ Passed: 421                          │
│ ❌ Failed: 4                            │
│ ⏭️ Skipped: 2                           │
│ Duration: 52.3s                         │
└─────────────────────────────────────────┘

Suites:
  ✅ Unit Tests (155/155)
  ✅ Integration Tests (254/256)
  ⚠️  E2E Tests (12/16)
```

## Дополнительная информация

- [Allure Framework Documentation](https://docs.qameta.io/allure/)
- [GitHub Pages Documentation](https://docs.github.com/en/pages)
- [GitHub Actions Artifacts](https://docs.github.com/en/actions/using-workflows/storing-workflow-data-as-artifacts)
